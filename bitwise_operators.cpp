//c program to use bitwise AND operator. By SlipBit.
#include<stdio.h>
int main()
{
    int a = 11 , b = 1 ;

    printf("a & b is: %d ",a & b);

    printf("\na | b is: %d", a | b);

    printf("\na ^ b is: %d", a ^ b);

    printf("\n~ (a) is: %d", ~ (a));

    return 0;
}
